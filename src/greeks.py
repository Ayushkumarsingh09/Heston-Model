import numpy as np

def delta(S, K, T, r, kappa, theta, sigma, rho, v0, option_type="call"):
    """Calculates Delta for the Heston Model."""
    # Implementation here

def gamma(S, K, T, r, kappa, theta, sigma, rho, v0, option_type="call"):
    """Calculates Gamma for the Heston Model."""
    # Implementation here

def vega(S, K, T, r, kappa, theta, sigma, rho, v0, option_type="call"):
    """Calculates Vega for the Heston Model."""
    # Implementation here

# Other Greeks can be added here...
